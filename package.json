{
  "name": "sandbot",
  "version": "2.0.0",
  "description": "Slack app for reserving sandboxes using Bolt Framework and SQLite",
  "main": "app.js",
  "scripts": {
    "bot": "node --use-openssl-ca app.js",
    "format": "eslint --fix .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "4.2.1",
    "sqlite3": "5.1.7"
  },
  "devDependencies": {
    "eslint": "^5.5.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0"
  }
}
